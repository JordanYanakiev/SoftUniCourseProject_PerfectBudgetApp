@using PerfectBudgetApp.Models.Expenses;

@model IEnumerable<AllExpensesViewModel>
@{
    ViewBag.Title = "All Expenses";
}


<div class="container" style=" margin-bottom:0px;  vertical-align: middle;">
      <div class="row justify-content-between">
        <div class="col-2" style=" vertical-align: middle;">
            <h1> Expenses </h1style>
        </div>
        <div class="col-2" style=" vertical-align: middle;">
            <form class="input-group-sm" asp-controller="Expense" asp-action="CreateExpense">
                <input class="mb-xl-4 btn btn-light mb-2 w-100 p-3 fw-bold" style="color:black; background-color:greenyellow;" type="submit" value="Add Expense" />
            </form>
        </div>
      </div>
</div>
<hr style="margin-top:0px;" />






@*<h2 class="text-center">@ViewBag.Title</h2>

<form class="input-group-sm" asp-controller="Expense" asp-action="CreateExpense">
    <input type="submit" value="Add Expense" class="fs-6 btn btn-success mb-3 p-2" />
</form>*@

@*<div class="text-center row">
    @foreach (var book in Model)
    {
        <div class="card col-4" style="width: 20rem; ">
            <img class="card-img-top" style="width: 18rem;"
             src="@book.ImageUrl" alt="Book Image">
            <div class="card-body">

                <h5 class="card-title mt-1">@book.Title</h5>
                <p class="mb-0">Author: @book.Author</p>
                <p class="mb-0">Rating: @book.Rating</p>
                <p>Category: @book.Category</p>
            </div>

            <form class="input-group-sm" asp-controller="Book" asp-action="AddToCollection" asp-route-id="@book.Id">
                <input type="submit" value="Add to Collection" class="fs-6 btn btn-success mb-3 p-2" />
            </form>
            <a href="/Book/Edit/@book.Id" class="btn btn-warning">Edit</a>
        </div>
    }
</div>
*@